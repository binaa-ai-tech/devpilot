# /binaa-metrics — Task Metrics

Summarize devpilot task history from `docs/tasks/` — totals, in-progress,
merged, a breakdown by command, and the most recent tasks.

```bash
bash scripts/metrics.sh
```

Use it to measure throughput and see whether the token-lean workflow is paying
off over time.
